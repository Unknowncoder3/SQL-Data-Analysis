--Create a table Employee with id, name, salary, department.
CREATE TABLE employee (
    emp_id INTEGER,
    emp_name TEXT,
    salary INTEGER,
    dept_id INTEGER,
    manager_id INTEGER,
    joining_date DATE
);
